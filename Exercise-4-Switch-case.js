var hari = 31;
var bulan = 12;
var tahun = 3000;

if(hari > 0 && hari <=31 && tahun >0 && tahun <= 10000){
    hari === hari;
    tahun === tahun;

    switch (true) {
    case ((hari === hari) && (bulan === 1) && tahun === tahun):
        console.log(`${hari} Januari ${tahun}`);
        break;
    case ((hari === hari) && (bulan === 2) && tahun === tahun):
        console.log(`${hari} Februari ${tahun}`);
        break;
    case ((hari === hari) && (bulan === 3) && tahun === tahun):
        console.log(`${hari} Maret ${tahun}`);
        break;
    case ((hari === hari) && (bulan === 4) && tahun === tahun):
        console.log(`${hari} April ${tahun}`);
        break;
    case ((hari === hari) && (bulan === 5) && tahun === tahun):
        console.log(`${hari} Mei ${tahun}`);
        break;
    case ((hari === hari) && (bulan === 6) && tahun === tahun):
        console.log(`${hari} Juni ${tahun}`);
        break;
    case ((hari === hari) && (bulan === 7) && tahun === tahun):
        console.log(`${hari} Juli ${tahun}`);
        break;
    case ((hari === hari) && (bulan === 8) && tahun === tahun):
        console.log(`${hari} Agustus ${tahun}`)
        break;
    case ((hari === hari) && (bulan === 9) && tahun === tahun):
        console.log(`${hari} September ${tahun}`);
        break;
    case ((hari === hari) && (bulan === 10) && tahun === tahun):
        console.log(`${hari} Oktober ${tahun}`);
        break;
    case ((hari === hari) && (bulan === 11) && tahun === tahun):
        console.log(`${hari} November ${tahun}`);
        break;
    case ((hari === hari) && (bulan === 12) && tahun === tahun):
        console.log(`${hari} Desember ${tahun}`)
        break;
    default:
        console.log('Input invalid');
    }
}else{
    console.log('Input invalid');
}







